# Minimal face deps - lighter, more compatible
# Works on Python 3.13 without compilation hopper
# Provides dlib 128-d fallback if ArcFace unavailable

# face_recognition package + dlib prebuilt wheels
# Try dlib-bin if dlib doesn't compile on your system
dlib-bin>=19.24.2 ; python_version < "3.13"
face-recognition>=1.3.0 ; python_version < "3.13"