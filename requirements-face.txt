# Optional face recognition deps - SOTA ArcFace
# Install ONLY if you want top-tier face matching
# Without these, FaceMatch falls back to OpenCV Haar + pHash

# ArcFace 512-d + RetinaFace (best)
insightface>=0.7.3
onnxruntime>=1.20.0

# dlib 128-d (fallback)
# Note: face-recognition requires cmake + C++ build tools on the system
# face-recognition>=1.3.0

# DBSCAN clustering (face grouping)
scikit-learn>=1.5.2